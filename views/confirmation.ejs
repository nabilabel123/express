<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmation</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome pour les icônes -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            padding-top: 50px;
        }
        .confirmation-card {
            max-width: 600px;
            margin: 0 auto;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
            border-radius: 10px;
            overflow: hidden;
        }
        .card-header {
            background-color: #28a745;
            color: white;
            text-align: center;
            padding: 1.5rem;
        }
        .success-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }
        .card-body {
            padding: 2rem;
        }
        .btn-return {
            border-radius: 50px;
            padding: 0.5rem 1.5rem;
        }
        .user-name {
            font-weight: bold;
        }
        .email-icon {
            color: #6c757d;
            margin-right: 8px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="confirmation-card card">
            <div class="card-header">
                <i class="fas fa-check-circle success-icon"></i>
                <h1 class="h3">Inscription réussie!</h1>
            </div>
            <div class="card-body text-center">
                <div class="alert alert-success mb-4" role="alert">
                    <p class="mb-0">Merci <span class="user-name"><%= nom %></span> pour votre inscription.</p>
                </div>
                
                <div class="email-notification mb-4">
                    <i class="fas fa-envelope email-icon"></i>
                    <p class="mb-0">Un email de confirmation a été envoyé à votre adresse.</p>
                </div>
                
                <div class="mt-4">
                    <a href="/inscription" class="btn btn-outline-secondary btn-return">
                        <i class="fas fa-arrow-left me-2"></i>Retour au formulaire
                    </a>
                </div>
            </div>
            <div class="card-footer text-center text-muted py-3">
                <small>Vous recevrez des instructions supplémentaires par email</small>
            </div>
        </div>
        
        <div class="text-center mt-4">
            <p class="text-muted">
                <small>En cas de problème, contactez le <a href="mailto:support@example.com">support</a></small>
            </p>
        </div>
    </div>
    
    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>